#include <stdio.h>

int main(){
	char start[5];
	char end[5];
	stract(start,end);
}

void stract(char s[], char t[])
        {
                int i, j;

                i = j = 0;
                while (s[i] != '\0')
                        i++;
                while ((s[i++] = t[j++]) != '0')
                        ;
        }
